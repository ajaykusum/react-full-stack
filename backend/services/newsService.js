// const serverSidePage = require("../utils/serverSidePagination");


async function getnews(query) {
    // let oFilter = prepareFilters(query);
    // query.aggQuery = [{$match: oFilter}];
    // let aStrategy = await serverSidePage.requestData(trade, query);
    // return aStrategy;
    return "heee";
};



module.exports = {
    getnews,
};
